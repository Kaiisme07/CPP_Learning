#include <iostream>
using namespace std;
	void reverse(int n, int a[]){
		int i = 0; 
		int j = n - 1;
			while (i < j){
				int temp;
				temp = a[j];
				a[j] = a[i];
				a[i] = temp;
				i++;
				j--;
				
			/*	for ( int i = 0 ; i < n/2 ; i++){
					int temp;
					temp = a[n-1-i];
					a[n-1-i] = a[i];
					a[i] = temp;;
													
				}
					*/
			}
			for ( int i = 0; i < n; i++){
				cout << a[i] << " ";
			}
	}
	
int main(){
	int n; cin >> n;
	int a[100];
		for ( int  i = 0; i < n; i++){
			cin >> a[i];}
	reverse(n, a);
	return 0;
	}
