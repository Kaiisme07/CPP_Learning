#include <iostream>
using namespace std;

bool palindrome(int n){
	int org = n;
	int res = 0;
		while (n){
			res = res * 10 + n % 10;
			n /= 10;
		}
		return org == res;
}


int main(){
	int n; cin >> n;
	cout << boolalpha << palindrome(n);
	return 0;
	}
